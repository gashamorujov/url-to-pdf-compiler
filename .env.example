# Optional: URL of a deployed CORS proxy backend (for production static hosts
# that do not ship the bundled Netlify/Vercel functions).
# The app appends ?url=<encoded-image-url> to this endpoint.
# VITE_CORS_PROXY=https://your-proxy.example.com/proxy

# Standalone proxy server (npm run proxy)
# PORT=8787
